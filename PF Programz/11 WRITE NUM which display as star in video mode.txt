.Model SMALL
.STACK 100H
.CODE

MAIN PROC

	MOV AX,0B800H
MOV ES,AX

MOV AL,'*'
MOV AH,07
MOV ES:[SI],AX


	MOV DS,AX
	MOV BX,0
	MOV AH,2
mov dx,0
	
WHILE1:
	CMP dx,158
	JE END1
MOV AH,2
	MOV AL,'*'
	MOV AH,07
	MOV ES:[SI],AX
	add si,2
	MOV DL,AL
	mov dh,' '  
	INT 21H
	MOV CX,65535
	FOR1:
		LOOP FOR1

	MOV AH,1
	INT 21H

	Inc dX
	JMP WHILE1

END1:
MOV AH,1
INT 21H
	
MOV AH,4CH
INT 21H
MAIN ENDP

END MAIN
          